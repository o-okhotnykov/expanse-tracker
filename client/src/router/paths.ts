export enum Paths {
  default = "/",
  dashboard = "/dashboard",
  history = "/history",
  login = "/login",
  register = "/register",
}

export enum PathsNames {
  default = "/",
  dashboard = "dashboard",
  history = "history",
  login = "login",
  register = "register",
}
